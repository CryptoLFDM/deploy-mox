---
#==================================================================
# Loki
#==================================================================
- hosts: gorgons
  roles:
    - role: user
      user_name: "{{ loki_owner }}"
      user_group: "{{ loki_group }}"
    - role: loki
      tags:
        - loki
