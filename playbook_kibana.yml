---
## --------------------------------------------------------------------
## Kibana
## --------------------------------------------------------------------
- hosts: kibana
  tags:
    - kibana
  roles:
    - role: user
      user_name: kibana
      user_group: kibana
    - role: filesystem
      filesystem_list: "{{ kibana_filesystem_list }}"
    - role: kibana


